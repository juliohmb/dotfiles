---
- name: get repos
  hosts: localhost
  connection: local
  tasks:
  # get AI repo
    - name: Clone the repository
      git:
        repo: git@github.com:juliohmb/essentials.git
        dest: ~/essentials
  